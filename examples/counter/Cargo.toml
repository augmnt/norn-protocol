[package]
name = "norn-counter"
version = "0.15.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
norn-sdk = { path = "../../norn-sdk" }
borsh = { version = "1.5", default-features = false, features = ["derive"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
